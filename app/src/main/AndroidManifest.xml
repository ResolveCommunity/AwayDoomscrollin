<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:label="@string/app_name"
        android:supportsRtl="true">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.AwaySplashScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".AntiCheatGuiltActivity"
            android:exported="false"
            android:theme="@android:style/Theme.Translucent.NoTitleBar" />

        <activity
            android:name=".ClearTaskInstagramActivity"
            android:exported="false"
            android:taskAffinity="com.instagram.android"
            android:theme="@android:style/Theme.NoDisplay" />

        <activity
            android:name=".ClearTaskTiktokActivity"
            android:exported="false"
            android:taskAffinity="com.zhiliaoapp.musically"
            android:theme="@android:style/Theme.NoDisplay" />

        <activity
            android:name=".ClearTaskYoutubeActivity"
            android:exported="false"
            android:taskAffinity="com.google.android.youtube"
            android:theme="@android:style/Theme.NoDisplay" />

        <service
            android:name=".AntiScrollService"
            android:exported="true"
            android:label="AwayDoomscrollin\'"
            android:permission="android.permission.BIND_ACCESSIBILITY_SERVICE">
            <intent-filter>
                <action android:name="android.accessibilityservice.AccessibilityService" />
            </intent-filter>
            <meta-data
                android:name="android.accessibilityservice"
                android:resource="@xml/accessibility_service_config" />
        </service>
    </application>
</manifest>
